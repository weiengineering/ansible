BGP Route Reflector Policy Panel:

The BGP Instance level policy is used to configure MP-BGP policies inside the fabric. The MP-BGP Route Reflector End Point Policy, which is used to specify Route Reflector Nodes/Spines for the MP-BGP running inside the fabric. Following example shows the details of an BGP Router Reflector Policy.
